# 项目 AI 工作约定

> GPT-6 适配变更说明：Astra low 与 Luna max 共用目标、边界和验收规则；删除 OMX 路由和重复确认流程，保留权限、研究协议与结果证据。Skill 按任务适用，验证按影响范围选择。

## 优先级与范围

在系统和开发者指令、沙箱权限及安全边界内：用户当前任务 > 当前项目具体规则 > 通用 Skills 和角色提示。目录规则只约束所在子树；具体研究协议不因通用自主执行原则而放宽。

这是多项目研究工作区。`s2c/AGENTS.md` 保存 S2C 知识与实验约束；`textoir/` 是独立仓库。`assets/`、`artifacts/`、`archives/` 是本地数据、原始产物和历史材料，不能作为普通清理目标。

## 执行与完成

- 已授权、信息充分且风险低的任务直接完成。先用相关代码、文档和 Git 历史解决常规细节，不依赖用户即时回答，不要求完整阅读仓库。
- 重大设计分歧、不可逆操作、生产环境变更、安全风险或明显超出任务范围且现有授权不足时才询问。权限审批仍由运行环境执行，不绕过沙箱。
- 普通错误先定位、修复并验证；方法失败后尝试合理替代路径。只有用户要求停止、任务完成，或缺少必要权限/输入且安全替代路径已穷尽时才停止。说明具体阻碍及已尝试的办法。
- 默认单人直接执行。仅为独立且有明确收益的子任务委派，给出范围与验收条件；不要因任务含“优化”“分析”“清理”就启动多 Agent 流程。
- 最小改动，复用现有实现；不顺手重构、不添加无关抽象或依赖、不覆盖用户已有修改。清理前简述改动范围和验证方法即可，不强制另建计划文件。
- 验证与改动风险相称：文档/提示词检查差异、路径与规则一致性；代码运行受影响测试及适用的 lint/typecheck；跨模块契约变更再扩大验证。行为修复或重构应有针对性回归证据，不为纯文字修改编写代码测试。
- 完成条件：请求范围内的改动已落地、必要检查通过、diff 无无关变化，并报告文件、验证证据和实际剩余风险。失败则修复重跑受影响检查；不因仓库无关旧问题无限扩展任务，也不把未运行的检查报为通过。

## Skills 与角色提示

- 明确的 `$name` 调用优先；自动使用仅限任务目的和产物匹配的 Skill。引用、讨论关键词或配置中的例子不构成调用。已禁用或不可用的 Skill 不因旧路由表而自动启用。
- 简单任务不强制经历 interview → plan → team/ralph；用户已要求“审查并修改”时，普通计划或审查阶段结束后继续实现与验收，不重复征求同一授权。
- 采用 Skill 前读取其入口；相关详细参考按需加载，已读且未变化的内容不反复读取。安全要求、数据协议、特殊 API 前置条件与必要验收仍须遵守。
- `response-discipline`、`karpathy-guidelines`、`kill-slop` 的通用建议合并为以上最小改动原则；它们不增加每次回复重读、普通不确定性必须提问、固定文件数或全量测试门槛。
- 学术 Skills 按产物选择：检索、全文阅读、写作、润色、配图或审稿各取所需；普通 ML 任务不默认套用 Nature/CNS 期刊限制。Azure/Hugging Face 云训练仅适用于明确的对应平台任务，本地 S2C 训练不触发云作业流程。
- Adobe/Canva/Figma 等外部应用仅在任务需要对应应用时使用；不因“画图/设计”等泛词自动创建外部文件。安装 Skill、改连接或发布内容需要属于用户授权范围。
- 角色提示用于分工，不额外扩大权限或制造批准步骤。原生子 Agent 优先继承当前可用模型；不要复制旧的固定模型表。模型不支持时回退到可用的继承配置或直接完成任务。
- 本项目不使用 OMX；不调用其命令、状态服务、工作流或旧角色提示。原生工具与必要的原生子 Agent 足够，不为普通任务新增状态框架。
- Astra low 与 Luna max 使用相同完成标准。默认 Astra low；需要委派时可按任务选择 Luna max，并显式传递目标、相关路径、特殊约束和验收要求；不强制双模型互审，不依赖父会话中的隐含知识。
- Skill 中的平台专属工具名使用当前环境等价能力；脚本路径相对实际 `SKILL.md` 所在目录解析。缺少可选工具先使用安全替代方案，不因工具名称、路径或任务跟踪器差异停止整个任务。

本次配置审查不自动提交或推送。详细配置审查记录见 `AI_CONFIG_REVIEW.md`，仅在维护 AI 配置时读取。
