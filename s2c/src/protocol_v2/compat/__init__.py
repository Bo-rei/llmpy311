"""Protocol_v2 data-format adapters for external method implementations.

These adapters deliberately expose only fixed-split exporters.  They neither
execute an external method nor choose Known labels, so every method remains
bound to the same protocol_v2 registry and views.
"""
