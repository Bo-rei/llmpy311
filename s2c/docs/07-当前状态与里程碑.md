# 当前状态与里程碑

更新时间：2026-07-20

本文件回答一个问题：现在做到哪一步、哪些结果可以写进论文、哪些还不能外推。

## 1. 当前源码状态

| 项目 | 当前状态 | 说明 |
| --- | --- | --- |
| 基础 Pipeline | 可运行 | src/cli.py 提供 prepare/train/evaluate/ablate 计划 |
| Gate/Router/Expert 核心实现 | 已在 main | 代码位于 src/ |
| v19/v20/v21 研究入口 | 已在 main | 统一入口位于 tools/experiments/cluster_separability/ |
| Cascade 修复入口 | 已在 main | `tools/eval/run_cascade_repair.py`；固定 KIR50/seed42，结果独立写入 `cascade_repair/gpu_kir50_seed42/` |
| CE-Recon/Cascade 收口工具 | 当前 `main` 工作树 | 入口已在当前工作树；不提交或推送 `autoresearch` |
| 数据/模型 | 工作区可见 | ../assets/，不纳入 Git |
| 实验产物 | 已生成 | ../artifacts/s2c/outputs/experiments/，跨分支保留 |

## 2. 研究主线已经完成什么

### Gate-only 主体

- 完成 v19 KIR × K_gate × distance × seed 主体比较；
- 完成单中心、多中心、Euclidean、per-cluster diagonal Mahalanobis；
- 完成受控 MSP/Energy/Entropy/kNN/LOF 和几何 Baseline；
- 完成 selected K、near/medium/far OOS、overlap、稳定性和效率诊断；
- 完成 Frozen MiniLM 的邻域纯度、类内/类间距离、意图 margin、线性 probe；
- 完成 Frozen/CE/SupCon 的固定边界表示对照；
- 完成表示碰撞与边界过覆盖分解；
- 完成 KMeans 与随机分簇等替代解释分析。

### 收口和外部验证

- 三个数据集、KIR50、seed42 的 12 个代表性 Cascade 单元已由修复后的下游组件生成；以 `cascade_repair/gpu_kir50_seed42/evaluations/matrix_manifest.json` 和 `cascade_summary.csv` 为准；
- 完整 KIR50 三 seed × 四 Gate 矩阵已完成 36/36 个单元；以 `cascade_full/gpu_kir50/evaluations/matrix_manifest.json`、`cascade_summary.csv` 和 `cascade_error_decomposition.csv` 为准；
- 外部 hard-negative 已覆盖 CLINC150 和 BANKING77-OOS；
- MOGB 已完成协议/依赖审计，但尚未获得公平复现数字；
- 既有 `study_closeout` 已登记 Gate-only/外部验证结果清单；本轮 12 单元修复和 36 单元矩阵均有独立 provenance，待论文数字冻结后再合并到总收口台账。

## 3. 当前最可靠的科学结论

1. near-OOS 主要表现为 Known 语义支持区域内部的表示碰撞，单纯扩展 K 或半径的边际收益有限。
2. CE/SupCon 往往改善 K=1，但不保证 K=2；因此“多簇普遍有效”不能作为结论。
3. StackOverflow 的 K>1 退化跨 Frozen、CE、SupCon 出现，说明固定 KMeans 多球与该数据的支持结构不匹配。
4. Gate-only 的差异可以传到完整系统，但后段 Router/Expert 和训练预算会显著改变最终结果；因此本轮先用 GPU 重训 Banking77/StackOverflow Expert，再解释新 Cascade 数字。
5. 外部 hard-negative 的结论不是单向的：CE-Recon 在 CLINC150 改善，在 BANKING77-OOS 的 K=1 hard-negative F1 低于 Frozen。
6. `best_controlled_baseline` 的 LogisticRegression pickle 来自较旧 sklearn 环境；当前评估有兼容性 warning，因此该行目前是参考结果，不是无条件的最终优胜结论。

## 4. 代表性 Cascade 的证据边界

当前代表性结果范围：

~~~
dataset ∈ {CLINC150, BANKING77-OOS, StackOverflow}
KIR = 50
data_seed = 42
Gate ∈ {
    Frozen K=1,
    Frozen validation-selected K,
    CE-Recon validation-best K,
    validation-best controlled baseline
}
~~~

共 12 个单元，四种 Gate 共用同一数据集下的 Router/Expert；Banking77/StackOverflow 使用本轮 GPU 重训的 Expert，CLINC150 使用已审计组件。

本轮下游单点结果：Banking77 Expert 测试准确率 `0.8490`，StackOverflow Expert 测试准确率 `0.8744`。这解释了旧 smoke 中约 `0.122/0.246` 的异常低 Known 性能，旧数字不再用于新 Cascade 主表。

修复后的 Cascade 摘要（KIR50/seed42）：

| Dataset | Gate | OOS F1 | Known macro-F1 | Overall Acc. | ID Recall |
| --- | --- | ---: | ---: | ---: | ---: |
| CLINC150 | Frozen K=1 | 0.8816 | 0.7858 | 0.8331 | 0.7311 |
| CLINC150 | CE-Recon selected-K | 0.8954 | 0.7998 | 0.8533 | 0.7333 |
| CLINC150 | Best controlled baseline | 0.9142 | 0.8619 | 0.8753 | 0.8613 |
| BANKING77-OOS | Frozen K=1 | 0.8616 | 0.8237 | 0.7838 | 0.8770 |
| BANKING77-OOS | Frozen selected-K | 0.9048 | 0.7590 | 0.8400 | 0.7310 |
| BANKING77-OOS | CE-Recon selected-K | 0.9026 | 0.7769 | 0.8373 | 0.7630 |
| BANKING77-OOS | Best controlled baseline | 0.8977 | 0.8454 | 0.8348 | 0.8740 |
| StackOverflow | Frozen K=1 | 0.8436 | 0.8384 | 0.8107 | 0.8293 |
| StackOverflow | CE-Recon selected-K | 0.9054 | 0.8625 | 0.8771 | 0.8393 |
| StackOverflow | Best controlled baseline | 0.8610 | 0.8523 | 0.8294 | 0.8503 |

完整列（含 Gate false accept/reject、Router/Expert error 和 SHA256）见：

```text
../artifacts/s2c/outputs/experiments/cascade_repair/gpu_kir50_seed42/cascade_summary.csv
```

训练参数、checkpoint SHA256 和环境见同目录的 `repair_manifest.json`；不要只依据 README 中的四舍五入数字。

这张表仍然只用于解释 seed42 的修复过程。三 seed 的稳定性应读取完整矩阵汇总：

```text
../artifacts/s2c/outputs/experiments/cascade_full/gpu_kir50/cascade_summary.csv
```

完整矩阵的三 seed 均值±标准差如下（KIR50；不是 KIR25/KIR75）：

| Dataset | Gate | OOS F1 | Known macro-F1 | Overall Acc. |
| --- | --- | ---: | ---: | ---: |
| CLINC150 | Frozen K=1 | 0.8802±0.0100 | 0.7850±0.0036 | 0.8306±0.0116 |
| CLINC150 | CE-Recon selected-K | 0.9000±0.0075 | 0.7968±0.0087 | 0.8579±0.0088 |
| CLINC150 | Best controlled baseline | 0.9084±0.0107 | 0.8518±0.0193 | 0.8677±0.0127 |
| BANKING77-OOS | Frozen K=1 | 0.8482±0.0144 | 0.8246±0.0189 | 0.7674±0.0158 |
| BANKING77-OOS | Frozen selected-K | 0.8811±0.0280 | 0.7869±0.0248 | 0.8080±0.0379 |
| BANKING77-OOS | CE-Recon selected-K | 0.8948±0.0069 | 0.7921±0.0181 | 0.8270±0.0096 |
| BANKING77-OOS | Best controlled baseline | 0.8417±0.0498 | 0.8401±0.0186 | 0.7657±0.0606 |
| StackOverflow | Frozen K=1 | 0.7902±0.0463 | 0.8398±0.0147 | 0.7673±0.0379 |
| StackOverflow | Frozen selected-K | 0.7800±0.0565 | 0.8411±0.0128 | 0.7590±0.0479 |
| StackOverflow | CE-Recon selected-K | 0.8762±0.0278 | 0.8668±0.0088 | 0.8485±0.0322 |
| StackOverflow | Best controlled baseline | 0.8324±0.0473 | 0.8562±0.0161 | 0.8072±0.0318 |

这组结果支持三个谨慎结论：CE-Recon 的完整系统收益在三个数据集的 OOS F1 上都较稳定，但不是所有数据集的 Known macro-F1 最优；Frozen 多簇在 StackOverflow 仍然退化；controlled baseline 的排名依赖数据集和 seed。它不支持“某个 Gate 全面最优”。

完整矩阵的错误阶段总计（36 个单元合计）为：`known_rejected_by_gate=13,702`、`oos_accepted_by_gate=16,706`、`known_wrong_domain=539`、`known_wrong_expert=4,052`；这些是计数汇总，不应当当作跨数据集平均错误率。

详细缺口见：

~~~
../artifacts/s2c/outputs/experiments/study_closeout/known_gaps.json
~~~

## 5. 结果入口

~~~
../artifacts/s2c/outputs/experiments/study_closeout/
├── closeout_manifest.json
├── result_inventory.csv
├── paper_source_map.csv
└── known_gaps.json
~~~

阅读顺序是 manifest → inventory → 汇总 CSV → source map → gaps。不要从旧文档里的数字开始。

## 6. 下一步的正确顺序

1. 将本轮 Cascade summary/provenance 登记进 closeout 快照；
2. 不再扩展 KIR25/KIR75 的四 Gate 矩阵，除非先补齐对应 CE-Recon 表示和下游组件协议；
3. 保留外部 hard-negative 的数据集差异，不把 CE-Recon 宣称为统一最佳；
4. 只有官方 MOGB 协议能复现并对齐 split，才进入主表；
5. 用已有结果做描述性机制关系和表图导出；
6. 数字冻结后再做最后的 active/legacy 代码清理。

## 7. 当前不应继续做的事

- 不再无目标增加 K、半径和普通 Baseline；
- 不再创建新的 v24/v25/... 文档入口；
- 不把研究分支产物当作 main 已复现结果；
- 不把 MOGB 审计状态当作性能数字；
- 不把 Gate-only F1 当完整意图识别 F1；
- 不在没有新证据时扩展复杂因果统计模型。
