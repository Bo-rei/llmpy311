# protocol_v2 test report

Latest verification command summary: `targeted paths/admission/runner/audit regression: 9 passed; broader suite remains pending because host filesystem I/O can block`.

This file records only the final command outcome; detailed pytest output is deliberately not committed.
