# TEXTOIR runtime independence

Status: **passed**. The check temporarily renamed `../textoir` to `textoir.disabled`, ran protocol validation, s2c/TEXTOIR-format export validation and Gate dry-run, then restored the directory and checked its Git status. No model training or embedding generation was used for this check.
