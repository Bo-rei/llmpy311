# s2c 项目完整说明（当前事实版）

更新时间：2026-07-20

这份文档是第一次接触 s2c 时应读的全景说明。它把源码、研究分支、实验产物和历史文档分开，避免把“能看到结果”误解为“当前 checkout 能复现结果”。

本文中的代码路径均以 `s2c/` 项目根为相对起点；项目根外的 `../assets` 和 `../artifacts`
是工作区目录，不是 `s2c/` 仓库内的目录。

## 1. 一句话理解

s2c 面向开放世界意图识别。输入先由 Gate 判断是否为已知意图（Known/ID）或未知意图（OOS），已知样本再由 Router 路由到领域，最后由 Expert 完成域内意图分类。

```text
文本 → Gate(OOS) → Router(domain) → Expert(intent)
```

论文修订的核心不是重写这条级联，而是研究冻结 MiniLM 语义空间中的 Known 多簇结构，以及这些局部支持区域能否改善 Known/OOS、尤其 near-OOS 的可分性。

## 2. 先分清三个时间层

### 2.1 当前 `main`：基础可运行源码和 Cascade 修复入口

当前 checkout 为：

```text
branch: main
```

它包含 `src/` 核心系统、v19 主训练/评估入口、v19/v20/v21 多簇诊断，以及本轮 `tools/eval/run_cascade_repair.py` 代表性完整 Cascade 入口。具体 commit 以运行目录中的 provenance 为准，不在文档中手工维护浮动 HEAD。

### 2.2 当前工作树：研究收口和完整 Cascade 入口

本轮 CE-Recon/表示碰撞、边界形状、外部 hard-negative、Cascade 修复和完整矩阵的编排器
已经放在当前 `main` 工作树的 `tools/` 中。它们目前作为工作树改动保留，不提交或推送到
`autoresearch`；运行前以具体脚本和 manifest 为准，不要切换分支。

### 2.3 `../artifacts`：跨分支保留的结果

大文件和结果位于：

```text
../artifacts/s2c/outputs/experiments/
```

这些目录不会随 `git switch` 自动删除。因此在 `main` 上看到 `v22` 或 `study_closeout` 是正常的，但不能据此推断当前 checkout 有生成它们的全部源码。结果来源必须以 manifest 的 `source_commit`、`git_branch` 和 hash 为准。

## 3. 代码结构：按职责阅读

```text
s2c/
├── src/
│   ├── gate/            Gate/OOS 检测器和语义门控
│   ├── router/          domain 路由
│   ├── models/          Expert、共享组件和模型头
│   ├── pipeline/        Gate → Router → Expert 级联
│   ├── runtime/         WorkspacePaths 与 artifact 契约
│   └── utils/           数据/模型加载工具
├── tools/
│   ├── analysis/        v19 训练、评估、汇总和论文导出
│   ├── eval/            系统评估和逐样本输出
│   ├── experiments/     v19/v20/v21 研究命令包
│   ├── gate/            Gate 训练
│   ├── train/           Router/Expert 训练
│   └── compat/textoir/  TextOIR 外部协议兼容层
├── scripts/data/active/  数据重建和审计入口
├── configs/              profiles、artifact 和运行配置
├── tests/                单元、协议和 smoke 测试
└── docs/                 项目文档
```

判断脚本是不是当前入口，优先检查：

1. `src/cli.py` 是否调用它；
2. `tools/experiments/cluster_separability/__main__.py` 是否注册它；
3. 结果 manifest 是否登记它的路径和 commit。

文件名带 `v19` 表示它遵守 v19 数据/输出协议，不等于“废弃”；文件名带 `v20`/`v21` 也不等于“主入口”，仍需查统一 CLI 和 manifest。

## 4. 运行时路径

`src.runtime.WorkspacePaths` 从项目向上查找包含 `src/`、`configs/` 的项目根，再查找同时包含 `assets/`、`artifacts/` 的工作区根。当前实际路径是：

```text
项目根：   /home/bo/bo01/llmpy311/s2c
工作区根： /home/bo/bo01/llmpy311
数据根：   ../assets/datasets/s2c
模型根：   ../assets/models
产物根：   ../artifacts/s2c
```

准备好的 v19 数据实际位于：

```text
../assets/datasets/s2c/prepared/data/multidataset/v19/<dataset>/kir<25|50|75>_seed<13|42|87>/
```

旧文档中的 `data/current` 是历史逻辑名称，不要在当前仓库新建同名目录，也不要把机器绝对路径写入代码。

## 5. 数据协议

主数据集：

```text
clinc150
banking77_oos
stackoverflow
```

常见控制变量：

```text
KIR ∈ {25, 50, 75}        # 已知意图比例
data_seed ∈ {13, 42, 87}  # Known 集合/切分种子
K_gate ∈ {1, 2, 3, 4, 5}  # 每个意图的局部中心数
distance ∈ {euclidean, mahalanobis_diag}
```

`KIR`、`K_gate` 和聚类诊断参数不是一回事。统一评价约定是：OOS 为正类，score 越大越倾向 OOS；K、半径、阈值和 checkpoint 只由 train/validation 选择；test 只做最终评估；Gate-only 和完整 Cascade 分表。

## 6. 研究阶段与回答的问题

### v19：主网格和受控 Gate

运行 KIR × `K_gate` × distance × seed 网格，比较单中心、多中心、Euclidean、per-cluster diagonal Mahalanobis，以及统一 MiniLM 表征上的 MSP/Energy/Entropy/kNN/LOF 等 Baseline。它建立可比较的 Gate-only 锚点，不证明“多簇永远有效”。

### v20：排除多簇收益的替代解释

增加 KMeans 与 size-matched random partition、自适应局部边界、selected K 可靠性、near/medium/far OOS、overlap 和效率分析，用来判断收益来自语义聚类还是单纯增加局部边界。

### v21：MiniLM 语义空间诊断

计算邻域 Purity@5/10/20、类内/类间几何、最近意图 margin、线性/kNN probe，并把 near-OOS 错误分成 representation collision 与 boundary overcoverage。再比较 Frozen、CE、SupCon 表示下的 K=1/K=2 行为。

### 研究收口

内部 Gate-only 证据之后，研究分支集中补四类外部闭环：

1. 固定 Router/Expert 上的代表性 Cascade；
2. 不参与训练和调参的外部 hard-negative OOS；
3. MOGB 协议和可复现性审计；
4. provenance、机制相关性和论文表图来源。

## 7. 当前取得的结果

以下 Cascade 数字来自当前 main 上的 GPU 修复产物；完整 provenance 在 `../artifacts/s2c/outputs/experiments/cascade_repair/gpu_kir50_seed42/repair_manifest.json`。Gate-only 和外部 hard-negative 的历史数字仍按各自 manifest 读取。

### 7.1 Gate-only 的稳定方向

- Frozen/CE/SupCon 的三数据集固定边界矩阵已经完成；
- CE/SupCon 通常改善 K=1 的 OOS/near-OOS，但不保证 K=2 在所有数据集有效；
- StackOverflow 在 Frozen、CE、SupCon 下的 K=2 都退化，说明固定 KMeans 多球不是普适支持模型；
- near-OOS 大量落在 Known 语义支持区域内部，表示碰撞是当前主要瓶颈；
- 继续增大 K、半径或阈值往往只是交换 Known false reject 与 OOS false accept。

### 7.2 代表性 Cascade（KIR50/seed42）

四个代表 Gate 共用同一数据集下的 Router/Expert：

| Dataset | Gate | OOS F1 | Known macro-F1 | Overall acc | ID Recall |
| --- | --- | ---: | ---: | ---: | ---: |
| CLINC150 | Frozen K=1 | 0.8816 | 0.7858 | 0.8331 | 0.7311 |
| CLINC150 | Frozen selected K | 0.8816 | 0.7858 | 0.8331 | 0.7311 |
| CLINC150 | CE-Recon selected K | 0.8954 | 0.7998 | 0.8533 | 0.7333 |
| CLINC150 | Best controlled baseline | 0.9142 | 0.8619 | 0.8753 | 0.8613 |
| BANKING77-OOS | Frozen K=1 | 0.8616 | 0.8237 | 0.7838 | 0.8770 |
| BANKING77-OOS | Frozen selected K | 0.9048 | 0.7590 | 0.8400 | 0.7310 |
| BANKING77-OOS | CE-Recon selected K | 0.9026 | 0.7769 | 0.8373 | 0.7630 |
| BANKING77-OOS | Best controlled baseline | 0.8977 | 0.8454 | 0.8348 | 0.8740 |
| StackOverflow | Frozen K=1 | 0.8436 | 0.8384 | 0.8107 | 0.8293 |
| StackOverflow | Frozen selected K | 0.8436 | 0.8384 | 0.8107 | 0.8293 |
| StackOverflow | CE-Recon selected K | 0.9054 | 0.8625 | 0.8771 | 0.8393 |
| StackOverflow | Best controlled baseline | 0.8610 | 0.8523 | 0.8294 | 0.8503 |

Banking77 Expert 的测试准确率为 `0.8490`，StackOverflow Expert 为 `0.8744`；旧的约 `0.122/0.246` 只属于历史低预算 smoke。修复后，Gate 差异可以在同一套下游组件上被公平观察；12 个单元仍不是三 seed、KIR25/75 的稳定性证明。

### 7.3 外部 hard-negative

外部 hard-negative 只用于测试，未进入训练、validation、K 或阈值选择；当前覆盖 CLINC150 和 BANKING77-OOS，StackOverflow 没有兼容公开文件。

| Dataset | 方法 | 普通 OOS F1 | Hard-negative OOS F1 | Hard OOS Recall |
| --- | --- | ---: | ---: | ---: |
| CLINC150 | Frozen K=1 | 0.8816 | 0.5214 | 0.9197 |
| CLINC150 | CE-Recon K=1 | 0.8954 | 0.5356 | 0.9665 |
| BANKING77-OOS | Frozen K=1 | 0.8616 | 0.3366 | 0.9046 |
| BANKING77-OOS | CE-Recon K=1 | 0.9165 | 0.3066 | 0.8924 |
| BANKING77-OOS | CE-Recon selected K | 0.9026 | 0.3221 | 0.9673 |

不能把 CE-Recon 写成所有外部数据都更好：它在 CLINC150 有改善，但在 BANKING77-OOS 的 K=1 hard-negative F1 低于 Frozen。

### 7.4 MOGB

MOGB 目前只有协议和依赖审计，没有可放入主表的公平复现数字。阻塞点包括官方 TSV 数据、BERT 模型以及 s2c split/label mapping 对齐。简化 adaptive boundary 不能称为官方 MOGB。

## 8. 结果的正确阅读顺序

进入：

```text
../artifacts/s2c/outputs/experiments/study_closeout/
```

按以下顺序读取：

1. `closeout_manifest.json`：分支、commit、总体状态和测试摘要；
2. `result_inventory.csv`：结果路径、来源 commit、split/hash 和用途；
3. 对应汇总 CSV/Parquet：真正的数值；
4. `paper_source_map.csv`：论文表/图来自哪个文件和列；
5. `known_gaps.json`：结果不能外推到哪里。

不要直接从一个 `predictions.json` 猜结论，也不要把 Gate-only OOS F1 当完整 intent macro-F1。

对于本轮完整 Cascade，优先读取：

```text
../artifacts/s2c/outputs/experiments/cascade_repair/gpu_kir50_seed42/repair_manifest.json
../artifacts/s2c/outputs/experiments/cascade_repair/gpu_kir50_seed42/cascade_summary.csv
../artifacts/s2c/outputs/experiments/cascade_repair/gpu_kir50_seed42/cascade_error_decomposition.csv
```

## 9. 最小运行流程

在 `s2c/` 目录执行：

```bash
python -m src.cli --help
python -m src.cli prepare --dataset clinc150 --kir 50 --seed 42 --dry-run
python -m tools.experiments.cluster_separability --help
pytest tests/unit -q
```

`--dry-run` 只打印运行计划，第一次接触项目时先执行它。当前 `main` 的基础链为：

```bash
python -m src.cli prepare --dataset clinc150 --kir 50 --seed 42
python -m src.cli train --dataset clinc150 --kir 50 --seed 42 --device cuda
python -m src.cli evaluate --dataset clinc150 --kir 50 --seed 42 --device cuda
```

v19/v20/v21 研究入口：

```bash
python -m tools.experiments.cluster_separability grid --help
python -m tools.experiments.cluster_separability baseline --help
python -m tools.experiments.cluster_separability v20-analysis --help
python -m tools.experiments.cluster_separability v20-random --help
python -m tools.experiments.cluster_separability v21-semantic-probe --help
python -m tools.experiments.cluster_separability v21-adaptation --preflight
```

v22/收口命令仍属于本地历史分支，不是当前 main 的运行契约。

当前完整 Cascade 直接使用：

```bash
python tools/eval/run_cascade_repair.py
python tools/analysis/export_cascade_repair_summary.py \
  --input-root ../artifacts/s2c/outputs/experiments/cascade_repair/gpu_kir50_seed42
```

## 10. 已知风险和下一步

1. **工作树与产物分离**：`../artifacts` 里的结果跨分支保留；先读 manifest，再判断当前源码是否能生成它。
2. **Cascade 范围有限**：KIR50/三 seed/四 Gate 的 36 个单元已完成，KIR25/KIR75 尚未覆盖。
3. **MOGB 未复现**：审计状态不能当性能对比。
4. **统计是探索性**：三 seed 不足以支持强因果表述。
5. **历史文档冲突**：旧文件可能使用 `data/current`、旧入口或旧锚点；以本文件、manifest 和源码为准。
6. **缓存清理已完成**：已删除 Git 中跟踪的 Python `__pycache__/*.pyc`，并在工作区根 `.gitignore` 中禁止重新加入；不影响模型或实验结果。

下一步不是继续创建新的版本号，而是：冻结 closeout provenance，决定是否扩展 Cascade seed，保留外部 hard-negative 的边界结论，只有官方协议复现后才纳入 MOGB 主表，最后再清理脚本命名和缓存。

## 11. 当前进度的一句话判断

s2c 已完成“MiniLM 表示碰撞—多簇结构—near-OOS 可分性”的主体 Gate-only 研究，并完成了修复下游后的 12 个代表性完整级联单元和 KIR50 三 seed × 四 Gate 的 36 个完整 Cascade 单元；KIR25/KIR75 尚未覆盖，也没有公平可比的 MOGB 数字。现在应当使用本指南和 manifest 阅读、收口和复核，而不是继续增加孤立实验文件。
