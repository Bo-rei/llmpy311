# 工具入口索引

本文件只列“当前应该从哪里进入”。不要根据文件名自行拼接参数；所有新增收口入口都必须先查看 `--help` 和对应 manifest。

## 1. main 分支：基础可运行入口

| 入口 | 用途 | 主要产物 |
| --- | --- | --- |
| python -m src.cli prepare | 准备/审计 v19 split | ../assets/datasets/s2c/prepared/ |
| python -m src.cli train | 训练多数据集主模型 | ../artifacts/s2c/outputs/experiments/multi_dataset_v19/ |
| python -m src.cli evaluate | 运行主 benchmark | ../artifacts/s2c/outputs/experiments/multi_dataset_v19/ |
| python -m src.cli ablate | 运行已登记主消融 | 由 configs/artifacts.yaml 决定 |
| tools/analysis/run_multi_dataset_training_v19.py | 多数据集训练实现 | multi_dataset_v19 |
| tools/analysis/run_multi_dataset_benchmark_v19.py | 多数据集评估实现 | multi_dataset_v19 |
| tools/analysis/run_structure_backbone_ablation_v19.py | 结构/主干消融 | 对应 ablation artifact |
| tools/eval/eval_system_pipeline_v19.py | Gate → Router → Expert 评估 | system pipeline 结果 |
| tools/eval/run_cascade_repair.py | 固定下游组件的 12 个代表 Cascade 单元 | cascade_repair/gpu_kir50_seed42/ |
| tools/train/run_cascade_components.py | 准备/训练 KIR50 三 seed 的 Router/Expert 组件 | cascade_full/gpu_kir50/ |
| tools/eval/prepare_cascade_gates.py | 为同一批组件准备四种 Gate adapter | cascade_full/gpu_kir50/gates/ |
| tools/eval/run_cascade_matrix.py | 运行或复用 36 个完整 Cascade 单元 | cascade_full/gpu_kir50/evaluations/ |
| tools/analysis/export_cascade_repair_summary.py | 汇总完整 Cascade 指标和错误阶段 | cascade_full/gpu_kir50/cascade_summary.csv |
| tools/analysis/validate_router_confidence_threshold_v19.py | Router 置信度校准 | 校准报告 |

先查看帮助：

~~~
python -m src.cli --help
python -m tools.experiments.cluster_separability --help
~~~

第一次运行建议只用 dry-run：

~~~
python -m src.cli prepare --dataset clinc150 --kir 50 --seed 42 --dry-run
~~~

## 2. main 分支：v19/v20/v21 研究入口

统一入口：

~~~
python -m tools.experiments.cluster_separability <command> --help
~~~

| command | 回答的问题 | 当前输出根目录 |
| --- | --- | --- |
| grid | KIR × K_gate × distance 的主网格 | cluster_separability_v19/ |
| baseline | 统一 MiniLM 表征上的 Gate-only Baseline | cluster_separability_v19/ |
| analyze | 稳定性、near/far、overlap、表示分析 | cluster_separability_v19/ |
| export | 论文表格和完整性审计 | cluster_separability_v19/ |
| v20-analysis | K 选择、near-OOS、效率汇总 | cluster_separability_v20/ |
| v20-random | KMeans 与随机分簇对照 | cluster_separability_v20/ |
| v20-end-to-end | 端到端替换实验的 preflight/命令 | cluster_separability_v20/ |
| v20-adaptive | 同表征自适应局部边界 Baseline | cluster_separability_v20/ |
| v21-semantic-probe | MiniLM 邻域、几何和 near-OOS 诊断 | cluster_separability_v21/ |
| v21-casebook | 子簇代表句和关键词审计模板 | cluster_separability_v21/ |
| v21-adaptation | Frozen/CE/SupCon 表示对照 | cluster_separability_v21/ |

这些命令在 main 的统一 CLI 中注册；参数以具体 command 的 help 为准。

## 3. 历史收口工具（仅 provenance，不是当前入口）

以下入口来自本地历史研究来源（manifest 可能记录其分支名）。它们只用于追溯已有结果，
不是当前 `main` 的基础运行契约，也不会提交到 `autoresearch`：

| 入口 | 回答的问题 | 输出根目录 |
| --- | --- | --- |
| representation-analysis | MiniLM 碰撞、hard OOS 和表示/边界分解 | minilm_representation_analysis/ |
| representation-confirmation | 已选表示变换的 test confirmation | minilm_representation_analysis/test_confirmation/ |
| adapt | CE-Recon 表示保持适配 | minilm_representation_analysis/adaptation/ |
| boundary | Euclidean/diag/full shrinkage 边界形状 | minilm_representation_analysis/boundary_shapes/ |
| sample-efficiency | 每意图样本量与多簇稳定性 | sample_efficiency/ |
| mechanism | 碰撞、碎片化、样本量与 near-OOS 的描述关系 | mechanism_analysis/ |
| external-hard-negative | 外部 hard-negative zero-adjustment 评价 | external_validation/hard_negative_oos/ |
| pipeline-status | 组件 preflight、历史 Cascade 和 smoke 状态 | pipeline_validation/、end_to_end_smoke/ |
| study-closeout | 生成 provenance、来源映射和已知缺口 | study_closeout/ |
| tools/eval/representative_cascade.py | 四个代表 Gate 的固定下游推理 | end_to_end_representative/ |

当前建议使用 `main` 上的 `tools/eval/run_cascade_repair.py`，它使用本轮 GPU 重训的 Banking/StackOverflow Expert，避免误用研究分支中的旧 smoke 编排脚本。

如需核对本地 provenance（通常不需要切换）：

~~~
git status --short
git branch --list autoresearch/minilm-collision-20260718
~~~

仅查看结果时不要切换分支；直接按 `../artifacts/...` 目录中的 manifest 读取来源。

如果只是查看这些结果，直接读 ../artifacts/s2c/outputs/experiments/ 即可，不必切换分支。

## 4. 产物读取入口

研究收口优先看：

~~~
../artifacts/s2c/outputs/experiments/study_closeout/
├── closeout_manifest.json
├── result_inventory.csv
├── paper_source_map.csv
└── known_gaps.json
~~~

普通 Gate 单元优先读取 scores.parquet、eval_results.json、threshold_selection.json 和 cluster_metrics.csv。完整 predictions 只在代表配置保存。

## 5. 入口选择决策树

~~~
我要运行基础流程？
  └─ src.cli prepare/train/evaluate/ablate

我要补 v19/v20/v21 Gate-only 研究？
  └─ tools.experiments.cluster_separability

我要运行本轮修复后的完整 Cascade？
  └─ tools/eval/run_cascade_repair.py

我要追溯 v22/旧收口工具？
  └─ 只读 ../artifacts/，必要时本地切研究分支（不提交）

我只想查看已有数字？
  └─ 先读 study_closeout，再按 result_inventory 找 CSV
~~~

## 6. 入口治理

- 新入口必须注册到统一 CLI 或明确放在 analysis/eval 的单一职责目录；
- 历史脚本不因文件名被删除，先通过 manifest 记录来源和用途；
- 不建立新的 v24/v25 Markdown 或命令入口；
- 任何“结果可读但源码不可复现”的情况都要在文档中明确注明。
