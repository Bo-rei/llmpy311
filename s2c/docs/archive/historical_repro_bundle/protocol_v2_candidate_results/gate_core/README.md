# protocol_v2 Gate results

This directory contains only lightweight aggregates derived from immutable Gate run manifests and metrics. It does not contain text, embeddings, checkpoints or predictions. `MANIFEST.csv` records the source summary SHA256. Gate-only results must not be interpreted as full Cascade results.
